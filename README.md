game_of_life.go
===============

How to install 

1. clone project
2. export GOPATH
  
    ```bash
    $ cd game_of_life.go
    $ export GOPATH=`pwd`
    ```
    
3. run `go get` for install dependencies
